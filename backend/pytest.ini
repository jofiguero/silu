[pytest]
testpaths = tests
python_files = test_*.py
# Sin esto pytest solo agrega `tests/` al sys.path y el paquete `app` no se
# encuentra al importar desde los tests.
pythonpath = .
addopts = -q --strict-markers
